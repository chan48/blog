import React, { Component, PropTypes } from 'react';

class SamplePage extends Component {
  static propTypes = {
  };

  static defaultProps = {
  };

  constructor(props) {
    super(props);
  }

  state = {
  };

  render() {
    return (
      <div>
        <div>sample page without body</div>
      </div>
    );
  }
}

export default SamplePage;